name: BTCO current state one-shot

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  btco-current-state:
    if: github.head_ref == 'qa/btco-current-state-2026-08-21'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Capture one current first-party BTCO state
        run: python3 scripts/btco_current_state_probe.py
      - name: Upload immutable evidence
        uses: actions/upload-artifact@v4
        with:
          name: btco-current-state-2026-08-21
          path: artifacts/btco-current-state/
          retention-days: 1
          if-no-files-found: error
