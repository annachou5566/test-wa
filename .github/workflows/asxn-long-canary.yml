name: ASXN longer bounded persistent-browser canary

on:
  workflow_dispatch:
    inputs:
      duration_seconds:
        description: "Bounded canary duration"
        required: true
        default: "14400"
        type: choice
        options:
          - "300"
          - "1800"
          - "7200"
          - "14400"
  push:
    branches: [main]
    paths:
      - ".github/workflows/asxn-long-canary.yml"
      - "scripts/asxn_stage3a_canary.py"
      - "scripts/asxn_long_canary.py"
      - "run/asxn-long-canary.trigger"

permissions:
  contents: read

concurrency:
  group: asxn-longer-bounded-persistent-browser-canary
  cancel-in-progress: false

jobs:
  canary:
    runs-on: ubuntu-latest
    timeout-minutes: 270
    env:
      CANARY_DURATION_SECONDS: ${{ github.event_name == 'workflow_dispatch' && inputs.duration_seconds || '14400' }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install bounded public-probe dependencies
        run: python -m pip install --disable-pip-version-check playwright psutil

      - name: Static compile and contract self-test
        run: |
          python -m py_compile scripts/asxn_stage3a_canary.py scripts/asxn_long_canary.py
          python scripts/asxn_long_canary.py --self-test

      - name: Run bounded longer canary
        run: >-
          python scripts/asxn_long_canary.py
          --duration-seconds "${CANARY_DURATION_SECONDS}"
          --output artifacts/asxn-long-canary/summary.json

      - name: Upload sanitized longer-canary evidence
        if: always() && hashFiles('artifacts/asxn-long-canary/summary.json') != ''
        uses: actions/upload-artifact@v4
        with:
          name: asxn-long-canary-${{ github.run_id }}
          path: artifacts/asxn-long-canary/summary.json
          retention-days: 3
          if-no-files-found: error
